# Hoist shared lint/format tooling so all workspace packages
# can use the root eslint.config.js without duplicating deps.
public-hoist-pattern[]=@eslint/*
public-hoist-pattern[]=eslint
public-hoist-pattern[]=typescript-eslint
public-hoist-pattern[]=prettier
